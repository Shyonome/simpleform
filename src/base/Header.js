const Header = ({  }) => {
    return (
        <div>
            <header>
                <span>Create account</span>
                <span className = {`hidden`} >Result</span>
            </header>
        </div>
    );
}

export default Header