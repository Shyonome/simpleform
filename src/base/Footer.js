const Footer = ({  }) => {
    return (
        <div>
            <footer>
                Made with <span>React</span> at <span>Le Reacteur</span> by <span>Isaak</span>
            </footer>
        </div>
    );
}

export default Footer